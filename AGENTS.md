# Cocos MCP 项目规则

## 创建和整理项目资源（强制）

- Agent 创建场景、材质、Shader、预制体、模型、图片、音频或脚本前，必须查询 `asset.location`，先查找并复用工程已有的对应类型目录；只有不存在合适目录时才创建。禁止把生成文件散落在 `db://assets/` 根目录，禁止为每次任务重复创建同义文件夹。
- Creator 3 创建入口会自动归类根目录目标并通过 AssetDB 创建缺失目录。必须使用返回的 `assetLocation.url` 或资源实际 URL/UUID 继续操作，不能继续使用原先假定的根目录路径。明确指定的业务子目录会保留。
- Shader 优先复用 Shaders/Effects，材质复用 Materials，场景复用 Scenes，预制体复用 Prefabs，模型复用 Models/Meshes，图片复用 Textures/Images；匹配不区分大小写。多个候选先选层级浅的目录，再按路径稳定排序。不得把其他 Bundle 或 resources 当作普通共享目录。
- 用户要求“项目资源整理”时，先执行 `asset.organize.plan`。默认只整理 assets 根目录；递归整理必须与用户范围一致。Agent 根据 `rows` 和 `skipped` 审查用途、业务分组、字符串加载路径和相对引用，再使用相同参数及 `planHash` 执行 `asset.organize.apply`。计划过期必须重新读取，不得绕过守卫。
- 已经按类型整理的业务目录保持原位；不得为统一大小写或英文命名重建已有目录。名称冲突不得覆盖或自动加随机后缀。
- `resources`、Asset Bundle、第三方/编辑器插件目录、脚本、数据、Chunk 及有外部依赖的模型/字体不进行自动搬移。Agent 必须报告跳过原因，另行分析路径引用，不能声称这些资源已经整理。
- 所有已有资源移动必须通过 AssetDB `asset.move` 或整理能力，保留 UUID 和子资源元数据。禁止直接用 shell/fs 搬动、删除或重建 `.meta`；UUID 不变也不能证明字符串路径引用仍有效。
- 整理后检查逐项验证结果、未完成项和日志中的回滚源/目标；失败时先查明 pending 项当前 UUID，不得盲目重试整个计划。禁止为了清空目录删除无关资产。
- 测试脚本和示例也受以上规则约束；临时报告、截图、缓存、构建产物只放工程 `.codex-work/`，不得作为正式资源放入 assets。

## 实现边界

- 保持 Creator 2/3 的能力支持范围真实；未经对应原生 API 验证，不得标记跨版本已验证。
- 路径策略必须集中实现并有冲突、过期计划、符号链接、UUID 保留及部分失败测试，不能仅靠提示词约束。
